# Wan-family structural conversion qualification

Local product only; no registry/release publication. Architecture identity stays `wan`.
Primary source is the pinned ModelScope revision in source-contract.json. HF revision proves 22 core file identities, not provider metadata identity.
No inference or precision policy is introduced. VAE/UMT5 frozen offset rules reuse proven source SHA256 identities; legacy specs are unchanged.
Native converter reads verified upstream configs as inert data, creates one canonical graph, two immutable bindings and resolved generic step tables.
All conversion inputs in this directory are pinned by spec-files.sha256 and its compiled digest.
